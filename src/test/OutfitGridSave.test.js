import { render } from '@testing-library/react';
import OutfitGridSave from '../componentes/OutfitgridSave';

describe('OutfitGridSaves component', () => {
    test('renders componente de outfits guardados', () => {
        render(<OutfitGridSave/>);
      });
})